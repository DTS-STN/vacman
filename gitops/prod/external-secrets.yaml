apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: redis
  labels:
    app.kubernetes.io/name: redis
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  data:
    - secretKey: REDIS_PASSWORD
      remoteRef: { key: prod, property: REDIS_PASSWORD }

---

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: vacman-backend
  labels:
    app.kubernetes.io/name: vacman-backend
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  data:
    - secretKey: AZUREAD_CLIENT_SECRET
      remoteRef: { key: prod, property: AZUREAD_CLIENT_SECRET }
    - secretKey: DYNATRACE_API_KEY
      remoteRef: { key: prod, property: DYNATRACE_API_KEY }
    - secretKey: GCNOTIFY_API_KEY
      remoteRef: { key: prod, property: GCNOTIFY_API_KEY }
    - secretKey: MSSQL_PASSWORD
      remoteRef: { key: prod, property: MSSQL_PASSWORD }
    - secretKey: MSSQL_USER
      remoteRef: { key: prod, property: MSSQL_USER }
  target:
    template:
      data:
        APPLICATION_GCNOTIFY_API_KEY: '{{ .GCNOTIFY_API_KEY }}'
        MANAGEMENT_OTLP_METRICS_EXPORT_HEADERS_AUTHORIZATION: 'Api-Token {{ .DYNATRACE_API_KEY }}'
        SPRING_DATASOURCE_USERNAME: '{{ .MSSQL_USER }}'
        SPRING_DATASOURCE_PASSWORD: '{{ .MSSQL_PASSWORD }}'
        SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_MS_GRAPH_CLIENT_SECRET: '{{ .AZUREAD_CLIENT_SECRET }}'

---

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: vacman-frontend
  labels:
    app.kubernetes.io/name: vacman-frontend
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  data:
    - secretKey: AZUREAD_CLIENT_SECRET
      remoteRef: { key: prod, property: AZUREAD_CLIENT_SECRET }
    - secretKey: DYNATRACE_API_KEY
      remoteRef: { key: prod, property: DYNATRACE_API_KEY }
    - secretKey: SESSION_COOKIE_SECRET
      remoteRef: { key: prod, property: SESSION_COOKIE_SECRET }
  target:
    template:
      data:
        AZUREAD_CLIENT_SECRET: '{{ .AZUREAD_CLIENT_SECRET }}'
        OTEL_AUTH_HEADER: 'Api-Token {{ .DYNATRACE_API_KEY }}'
        SESSION_COOKIE_SECRET: '{{ .SESSION_COOKIE_SECRET }}'

---

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: group-sync
  labels:
    app.kubernetes.io/name: group-sync
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  data:
    - secretKey: CLIENT_SECRET
      remoteRef: { key: prod, property: AZUREAD_CLIENT_SECRET }
